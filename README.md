
# Mood Music AI (Static)

Mood-based music playlist recommender using Next.js and Tailwind CSS.
This version uses static song lists (no Spotify API keys required).

## Setup

1. Install dependencies:

```bash
npm install
```

2. Run the development server:

```bash
npm run dev
```

Then open http://localhost:3000 in your browser.
